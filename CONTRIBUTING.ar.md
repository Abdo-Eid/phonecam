# المساهمة في PhoneCam

> **In English:** [`CONTRIBUTING.md`](CONTRIBUTING.md)

يحوّل PhoneCam هاتف أندرويد إلى كاميرا ويب حقيقية عبر USB على ويندوز. المسار يعمل كاملًا على عتاد
حقيقي، والمشروع في الوقت نفسه غير مكتمل. وشقّا هذه الجملة مهمّان معًا: هناك مسار عامل تبني عليه،
وهناك عمل حقيقي ومفيد لم يُنجز بعد.

يغطّي هذا الدليل ما تحتاج معرفته قبل أول تعديل لك.

## قبل أي شيء: اقرأ سجل البنية

ملف [`docs/architecture.md`](docs/architecture.md) ليس وثيقة تصميم كُتبت مسبقًا، بل سجل جارٍ لما
بُني فعلًا وبالترتيب، بما في ذلك الأخطاء التي ظهرت في الطريق والقرارات التي تبيّن خطؤها لاحقًا. هو
ملف طويل، وهو أعلى ما يمكنك قراءته قيمةً قبل الاقتراب من هذه الشيفرة.

مثالان يوضّحان لماذا يستحق وقتك:

- رُفض USB tethering في مرحلة مبكرة لسببين مُعلنين، ثم سقط السببان معًا: أحدهما فقد صلاحيته بصمت
  مع تطوّر الشيفرة، والآخر لم يُختبر أصلًا. وهو اليوم قناة النقل الأساسية. ما زال الرفض القديم
  موجودًا في الوثيقة، موسومًا بأنه متجاوَز، مع إبقاء التعليل كما هو.
- تظهر الدقّات الرأسية تالفة في بعض التطبيقات. الخلل في مكوّن من ويندوز نفسه لا في هذه الشيفرة،
  وتسجّل الوثيقة الاختبارات الثلاثة التي حصرت السبب بدقة — حتى لا يكرّر أحد ذلك الجهد.

إن غيّرت شيئًا يصفه السجل، فحدّث السجل في التعديل نفسه.

## ما يسعى إليه المشروع

- **USB أولًا.** ليس منتجًا لاسلكيًا. زمن الاستجابة والموثوقية هما الغاية.
- **دون صلاحيات جذر ودون ارتباط بمصنّع.** يجب أن يعمل على هاتف عادي يملكه شخص عادي.
- **مجاني بلا نسخة مدفوعة.** لا علامة مائية، ولا ميزة محجوبة.
- **صريح بشأن حالته.** يذكر ملف README وهذا الموقع ما لم يُنجز بعد. أبقِ الأمر كذلك، ولا تصف شيئًا
  بأنه مكتمل وهو يعمل جزئيًا.

## خريطة المستودع

| المسار | ما هو |
| --- | --- |
| `android/` | بلغة Kotlin. التقاط عبر Camera2، وترميز عبر MediaCodec، وقنوات النقل، وواجهة الهاتف. |
| `windows/host/` | بلغة ⁦C++⁩. ملف `phonecam-host.exe` — قنوات النقل، وفكّ ترميز H.264، وواجهة شريط المهام. |
| `windows/vcam/` | ⁦C++/COM⁩. مصدر الوسائط `MFCreateVirtualCamera` الذي يحمّله Frame Server في ويندوز. |
| `windows/common/` | شيفرة ⁦C++⁩ مشتركة: ring buffer الإطارات بين العمليات، وتأطير البيانات، والتسجيل. |
| `windows/svc/` | خدمة صغيرة تملك دورة حياة الذاكرة المشتركة. |
| `windows/usbdriver/` | أداة مساعدة تعمل بصلاحيات المدير، تثبّت تعريف AOA وتزيله. للقناة الاحتياطية فقط. |
| `proto/` | مخططات FlatBuffers — العقد الذي يُبنى عليه الجانبان. |
| `docs/` | سجل البنية، ومواصفات البروتوكول، وتعليمات البناء، وموقع المشروع. |

**لا يتشارك جانبا ويندوز وأندرويد أي شيفرة.** العقد الوحيد بينهما هو صيغة النقل في `proto/`
والتأطير الموصوف في [`docs/wire-protocol.md`](docs/wire-protocol.md). وأي تغيير في أحدهما يستلزم
تغيير الطرفين وإعادة توليد الملفات.

## التهيئة

التفاصيل الكاملة في [`docs/build.md`](docs/build.md). باختصار تحتاج إلى أدوات بناء
Visual Studio 2022 مع حزمة ⁦C++⁩، وحزمة Windows 11 SDK، وCMake، ووجود `flatc` و`adb` في متغيّر
`PATH`، إضافةً إلى JDK 21 وحزمة Android SDK لجانب الهاتف.

```
git clone https://github.com/Abdo-Eid/phonecam
cd phonecam
git submodule update --init --recursive

cmake --build windows/build --target phonecam-host --config Debug
cd android && ./gradlew assembleDebug
```

أمران يقعان فيهما الجميع مرة واحدة:

1. **تُبنى الكاميرا الافتراضية عبر MSBuild لا CMake**، لأنها تحتاج حزم NuGet لا يستهلكها CMake
   بشكل طبيعي.
2. **الملف المسجَّل نسخة منشورة**، وليس ناتج البناء مباشرةً. إعادة البناء وحدها لا تحدّث ما
   يحمّله ويندوز. يشرح `docs/build.md` خطوة النشر وكيفية حلّها حين يكون الملف مقفلًا.

## اختبار تعديلك

لا يملك المشروع حزمة اختبارات آلية. هذه ثغرة حقيقية، وإضافة اختبارات مساهمة مرحّب بها. وإلى أن
يحدث ذلك، يبقى التحقق يدويًا، والمعيار هو: **اذكر ما لاحظته فعلًا.**

بعض الفحوص المفيدة:

```powershell
# هل تصل الكاميرا الافتراضية إلى تطبيقات DirectShow لا Media Foundation وحدها؟
# (نسخة Zoom التقليدية وإصدارات OBS القديمة تعتمد DirectShow. "تعمل في تطبيق الكاميرا" لا تثبت شيئًا.)
ffmpeg -f dshow -list_devices true -i dummy
ffmpeg -f dshow -list_options true -i video="PhoneCam (Windows Virtual Camera)"
ffmpeg -f dshow -i video="PhoneCam (Windows Virtual Camera)" -frames:v 1 -update 1 out.png
```

إن عدّلت قناة نقل، فاختبرها على عتاد حقيقي واذكر طراز الهاتف وإصدار أندرويد. كل ما جرى التحقق منه
حتى الآن كان على جهاز واحد — Redmi Note 8 بنظام أندرويد 11 — لذا فإن تقريرًا من عتاد مختلف يُعدّ
مساهمة قيّمة فعلًا حتى دون شيفرة مرفقة.

## طلبات الدمج

- **موضوع واحد لكل طلب دمج.** إصلاح في قناة نقل وتعديل في الواجهة طلبان منفصلان.
- **اذكر ما تحققت منه وما لم تتحقق منه.** عبارة «بُني ولم يُختبر مع هاتف حقيقي» مقبولة تمامًا،
  وأنفع بكثير من الصمت.
- **حدّث الوثائق في التعديل نفسه.** إن أصلحت ما يصفه `docs/architecture.md` بأنه معطّل، أو حقّقت ما
  يصفه بأنه متعذّر، فغيّره هناك أيضًا.
- **التزم بأسلوب الشيفرة المحيطة.** تشرح هذه الشيفرة *سبب* الاختيار، خاصةً حين يبدو غريبًا بسبب خلل
  حقيقي جرت مواجهته. الشيفرة المقتضبة دون تفسير لاختيار غير بديهي ستُقابَل بطلب توضيح في المراجعة.
- **لا تُعِد كتابة التاريخ في الوثائق بصمت.** حين يتبيّن خطأ قرار موثّق، ضع عليه وسم «متجاوَز» واشرح
  سبب فشله. هذا السجل هو أنفع ما في هذا المستودع.

## أسلوب الشيفرة

- **⁦C++⁩**: معيار C++20 مع MSVC. أربع مسافات للإزاحة، وخلوّ من تحذيرات `/W4`. فضّل الأنماط القائمة —
  انظر كيف تتشابه قنوات النقل الثلاث قبل ابتكار شكل رابع.
- **Kotlin**: مسافتان للإزاحة، والتزام بأعراف أندرويد المعتادة.
- **التعليقات تشرح السبب لا الفعل.** المعيار: هل يستطيع شخص بعد ستة أشهر أن يميّز إن كان هذا السطر
  مقصودًا أم عرضيًا؟

## الإبلاغ عن الأخطاء

اذكر طراز هاتفك وإصدار أندرويد، وإصدار ويندوز لديك، وقناة النقل المستخدمة، والمخرجات ذات الصلة من
`phonecam-host.exe`. عادةً يكون سطر السجل الذي يبيّن قناة النقل المختارة أنفع معلومة منفردة.

وإن كانت المشكلة عدم ظهور الكاميرا الافتراضية، فاذكر التطبيق الذي جرّبته — إذ تفشل تطبيقات
Media Foundation وتطبيقات DirectShow بطرق مختلفة، وهذا التمييز يضيّق دائرة البحث سريعًا.

## الرخصة

يخضع PhoneCam لرخصة [GPL-3.0 أو أحدث](LICENSE). بمساهمتك فأنت توافق على ترخيص عملك بالرخصة نفسها.
